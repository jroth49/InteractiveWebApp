<!DOCTYPE html>
<html>
    <body>
        <header>
            <title>Pickup Sports Charlotte</title>
            <h1>
                Pickup Sports
                <a href="/"><img src="/images/sportslogo.png" href="/"></a>
            </h1>
            
            <link rel="stylesheet" href="/css/style.css"/>
            <script src="page.js"></script>
    
            <div class="navBar">
                <%if(!user) {%>
                    <a href="/users/login">LOG IN</a>
                    <a href="/users/signup">SIGN UP</a>
                <%} else {%>
                    <a href="/users/profile">Profile</a>
                    <a href="/users/logout">Logout</a>
                    <a href="/stories/new">Start a New Connection</a>
                <%}%>
                
            </div>

            <div class="navBar2" id="connect">
                <p><a href="/stories">Connections</a></p>
            </div>
            
            
        </header>

        <%if(successMessages.length > 0) {
            successMessages.forEach(message => {%>
                <main><div id="success"><%=message%></div></main>
            <%})
        }%>
        <%if(errorMessages.length > 0) {
            errorMessages.forEach(message => {%>
                <main><div id="err"><%=message%></div></main>
            <%})
        }%>
        